@page "/orders"
@inject NavigationManager Navigation

@code {
    // Orders live inside the Make Studio workspace — the legacy standalone
    // layout is retired.
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/quotes?ws=orders", replace: true);
    }
}
