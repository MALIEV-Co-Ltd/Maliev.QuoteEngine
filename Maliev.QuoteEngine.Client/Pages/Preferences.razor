@page "/preferences"
@inject NavigationManager Navigation

@code {
    // Preferences live inside the Make Studio workspace settings page — the
    // legacy standalone layout is retired.
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/quotes?ws=settings&tab=general", replace: true);
    }
}
