@page "/profile"
@inject NavigationManager Navigation

@code {
    // Profile lives inside the Make Studio workspace settings page — the legacy
    // standalone layout is retired.
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/quotes?ws=settings&tab=profile", replace: true);
    }
}
