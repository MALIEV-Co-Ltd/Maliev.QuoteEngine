@page "/documents"
@inject NavigationManager Navigation

@code {
    // Documents live inside the Make Studio workspace — the legacy standalone
    // layout is retired.
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/quotes?ws=documents", replace: true);
    }
}
