@page "/ndas"
@inject NavigationManager Navigation

@code {
    // NDAs live inside the Make Studio workspace — the legacy standalone
    // layout is retired.
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/quotes?ws=ndas", replace: true);
    }
}
